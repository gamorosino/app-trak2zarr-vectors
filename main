#!/bin/bash

#SBATCH --job-name=app
#SBATCH --ntasks-per-node=1
#SBATCH --mem=4G
#SBATCH --time=01:00:00

# Generated by braise's scaffold_brainlife_app.py -- this is the entrypoint
# brainlife.io's orchestrator invokes directly (`./main`). It stages
# config.json into the working directory before running this.

set -e

export SINGULARITYENV_LC_ALL=C.UTF-8
export SINGULARITYENV_LANG=C.UTF-8

singularity exec -e docker://gamorosino/zarr-vectors-tools:latest python3 trak2zarr_vectors.py

# trak2zarr_vectors.py writes vectors/vectors.zarrvectors itself, matching
# the "vectors" output id and neuro/track/zarr-vectors' own dirname field.
[ -z "$(ls -A vectors 2>/dev/null)" ] && echo "no output produced" && exit 1
exit 0
